{
  "name": "resume-generator-all",
  "version": "1.0.0",
  "scripts": {
    "install-ui": "cd resume-generator-ui && npm install",
    "install-api": "cd resume-generator-api && npm install",
    "build": "npm run install-ui && cd resume-generator-ui && npm run build",
    "start": "cd resume-generator-api && node server.js"
  }
}