{
  "name": "resume-generator-all",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && cd resume-generator-ui && npm install && cd ../resume-generator-api && npm install",
    "build": "cd resume-generator-ui && npm run build",
    "start": "cd resume-generator-api && node server.js"
  }
}